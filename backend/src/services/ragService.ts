export const generateEmbedding = async (text: string) => { return [0.1, 0.2]; };// Configured Pinecone indexing client
// Add similaritySearch functions against indexed chunks
// Boost project and skills vectors inside metadata filter maps
