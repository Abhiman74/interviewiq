import { Request, Response } from 'express';
export const uploadResume = async (req: Request, res: Response) => { res.json({ id: 'resume_id' }); };// Integrate schema storage inside upload controller
